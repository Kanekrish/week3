# Prompt the user to enter the target number of steps.
target_steps = int(input("How far we from the target"))

# Use a for loop to count down the number of steps remaining.
for i in range(target_steps, 0, -1):
    print(f"steps remaining: {1}")
